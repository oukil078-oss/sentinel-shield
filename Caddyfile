{$DOMAIN:localhost} {
    # API routes
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Health check
    handle /health {
        reverse_proxy backend:8000
    }

    # Static frontend files
    handle {
        root * /srv/frontend
        file_server
        try_files {path} /index.html
    }
}
